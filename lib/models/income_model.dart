import 'package:flutter/material.dart';

class IncomeModel {
  final String value, title;
  final Color color;
  const IncomeModel({
    required this.color,
    required this.title,
    required this.value,
  });
}
